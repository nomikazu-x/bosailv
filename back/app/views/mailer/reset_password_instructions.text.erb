<%= @resource.name %> 様

<%= t('app_name') %> をご利用頂きありがとうございます。

下記のURLをクリックして、パスワードを再設定してください。
パスワード再設定の依頼をしていない場合は、このメールを無視してください。
※このURLの有効期限は<%= Settings['reset_password_within_hours'] %>時間です。
<% if message['redirect-url'].to_s.present? %>
<%= edit_auth_password_url(config: message['client-config'].to_s, redirect_url: message['redirect-url'].to_s, reset_password_token: @token) %>
<% else %>
<%= edit_password_url(@resource, reset_password_token: @token) %>
<% end %>
