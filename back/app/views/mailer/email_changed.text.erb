<%= @resource.name %> 様

<%= t('app_name') %> をご利用頂きありがとうございます。

<% if @resource.try(:unconfirmed_email?) %>
メールアドレスの変更を受け付けました。
下記の新しいメールアドレスにお送りしたメールを確認してください。
<%= @resource.unconfirmed_email %>
<% else %>
下記のメールアドレスへの変更が完了しました。
<%= @resource.email %>
<% end %>
