<%= @resource.name %> 様

<%= t('app_name') %> をご利用頂きありがとうございます。

ログイン失敗が繰り返された為、アカウントがロックされました。
アカウントロックを解除するには、下記のURLをクリックしてください。
※ロックは<%= Settings['unlock_in_hours'] %>時間後に自動で解除されます。
<% if message['redirect-url'].to_s.present? %>
<%= auth_unlock_url(config: message['client-config'].to_s, redirect_url: message['redirect-url'].to_s, unlock_token: @token) %>
<% else %>
<%= unlock_url(@resource, unlock_token: @token) %>
<% end %>
